# 对抗性审查技能

## 使用时机

在合并 MCL、安全、接口、实验或高级模型变更之前。

## 攻击清单

- 模型或格式错误的请求能否绕过安全治理器？
- ALLOW 是否绑定到一个未修改的动作/状态并会过期？
- 单位、frame、标定和实际回执是否已知？
- 前/后验证是否可能产生假成功？
- 该变更是否减少了已测量的失败，还是仅仅增加了技术？
- 模拟结果是否被错误标记为真实证据？
- 失败是否保留其数据并安全路由？

## 验收标准

每个高/严重级别的发现都要用测试修复、用证据明确拒绝，或在 `project_state.yaml` 中作为命名阻挡项。
