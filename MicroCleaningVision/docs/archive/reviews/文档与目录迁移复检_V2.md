# 文档与目录迁移复检 V2

复检日期：2026-08-15。

## 1. 复检对象

旧版参考：

- `docs/archive/legacy_docs/分工与任务文档.md`；
- `docs/archive/legacy_docs/开发计划.md`。

新版对象：目录结构、总导航、长期阶段规划、团队总分工、三份个人手册、任务看板、术语词典、Git教程、环境教程和当前状态。

## 2. 旧文档值得保留的优点

1. 能看到Phase 1到Phase 4的整体路线，不只写今天。
2. 每个人有具体文件、学习内容、操作步骤和完成标准。
3. 提供环境配置、Git、常见问题和路径速查，适合新人。
4. 把阶段入口、出口和风险写成显式内容。

新版已保留这些表达形式，但不保留未经证据的固定技术顺序。

## 3. 旧文档必须纠正的问题

| 旧问题 | 新版修正 |
|---|---|
| 默认OpenCV后必然YOLO、双目、3D | 改为P0～P7证据关卡；高级技术需要稳定失败和A/B |
| 直接把检测结果发送喷水命令 | 保留ActionRequest、最低安全边界、回执和复检 |
| 成员任务量严重不平衡 | 改为A数据模型、B视觉测量、C目标控制；每轮核心/扩展/备用任务 |
| C被安排环境、检查、保存和报告 | C主任务改为目标点、路线、坐标、FakeSTM32和未来串口 |
| 完全流程化，上游未完成下游等待 | B用合成色块、C用合成mask独立开始，之后再换真实上游 |
| 文件路径属于旧仓库结构 | 全部活动文档改为三模块和镜像测试目录 |
| 固定工时看似精确 | 改为任务点和实际专注时段回填，两轮后校准 |
| 依赖一次安装YOLO/Torch/串口全部内容 | 改为mock、OpenCV、串口三层最小环境 |
| 代码/文件容易被写成能力完成 | 各文档区分合成测试、真实像素、真实硬件和研究结论 |

## 4. 首轮自评和返回修改

首轮评分：8.7/10。

发现两个明显不足：

1. 当前 `ContaminationMeasurement` 仍使用 `uncertainty_mm`，与“未标定先输出像素误差”的文档原则冲突。
2. C的像素级多目标计划不能直接放入当前只接受 `work_mm` 单目标的 `ActionRequest`。

已返回修改：

- 在B任务看板、B手册和项目状态中新增像素/毫米不确定度接口债务和提案任务；没有批准前不改共享契约。
- 在C手册中明确像素计划先做内部预览；只有有效标定后才生成动作申请，多点动作另开接口提案。

## 5. 最终评分

| 维度 | 分数 | 证据 |
|---|---:|---|
| 新手导航 | 9.5 | 15分钟路线、按问题索引、两小时入门 |
| 长期阶段完整性 | 9.4 | P0～P7均含问题、三人任务、出口和停止条件 |
| 当前任务可执行性 | 9.2 | 三人各有步骤、输入、输出、测试和验收 |
| 并行性与防阻塞 | 9.5 | 合成fixture、备用任务和第一次集成卡 |
| 分工与文件所有权 | 9.5 | 三业务目录、三测试目录、共享文件规则 |
| 学习路线 | 9.3 | 三份手册按L0～L3分层，并有最小练习 |
| 术语解释 | 9.4 | 从科研、成像、视觉、控制到AI/Agent完整解释 |
| Git和换电脑教程 | 9.3 | 实际仓库、venv、分支、PR、冲突和常见错误 |
| 证据诚实性 | 9.7 | 明确30项测试边界、无真实数据/硬件、模型门槛 |
| 个性化程度 | 8.5 | 已使用A持有设备和GPU经验；B/C兴趣与能力未知 |

最终综合自评：**9.3/10**。

## 6. 自动与人工检查结果

- 目录迁移前：30/30测试通过。
- 目录迁移后：30/30测试通过。
- `compileall`：通过。
- 当前Markdown本地链接静态检查：0个断链。
- `inspect_images --help`：新模块入口可运行。
- 旧活动代码导入路径搜索：0处残留。

## 7. 仍然存在的限制

1. B/C个人兴趣、基础和可投入时间未知，只能先按角色任务包安排，再用实际专注时段校准。
2. A尚未提交真实USB显微镜数据，所以文档中的P1步骤仍是计划，不是完成事实。
3. C的目标点和路线代码尚未实现；这次只做目录迁移与文档体系，避免未经任务卡批准增加代码。
4. 像素/毫米不确定度接口债务尚未修改代码，需要三人接口评审。
5. 本轮没有提交或推送GitHub，所有变化仍在本地工作区。

## 8. 下一轮改进触发条件

- 三人完成第一轮后，用实际专注时段重新平衡任务点；
- B/C明确兴趣和基础后，再调整学习深度；
- A真实数据到位后，将示例任务中的假设替换成设备实际信息；
- 第一次集成后，把真实失败写入下一版任务卡，而不是继续增加文档篇幅。
