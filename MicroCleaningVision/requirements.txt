# 当前 P0 / Mock MCL 的基础环境：不需要第三方 Python 包。
#
# 保留此文件是为了让新成员仍可执行：
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# 当且仅当对应的真实任务卡开始时，再额外安装：
# - 相机与 OpenCV 基线：requirements/perception-opencv.txt
# - 已评审的 STM32 串口适配器：requirements/control-serial.txt
#
# 不要把历史 YOLO、PyTorch、3D 或训练依赖加到这里；它们不是当前闭环依赖。
