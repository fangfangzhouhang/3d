# 全团队唯一的当前事实。计划、愿景和代码文件不能自动成为事实。
current_phase: P0 - 视觉软件回放闭环框架
current_evidence_level:
  overall_hardware: E0
  software_components: E1
team_scope:
  in_scope:
    - 显微图像输入与质量评估
    - 污染分割、测量、状态估计与前后图复检
    - 软件回放闭环、FakeSerial、STM32 接口合同与 Episode
  out_of_scope:
    - 机械结构设计与加工
    - STM32 固件实现
    - 真实泵、电机、喷头和 XY 平台控制

validated_capabilities:
  - "旧 Mock MCL 的 16 项标准库测试通过；只证明合成流程与失败路由。"
  - "视觉软件框架的 12 项测试通过：图像质量、像素/毫米坐标隔离、状态估计、面积复检、安全拒绝、FakeSerial 和 Episode。"
  - "main.py 只运行 Mock，不再提供旧真实控制入口。"

unvalidated_claims:
  - 真实显微图像采集和质量阈值
  - PLA 彩色水溶性标记物的 OpenCV 分割效果
  - 前后图真实配准和复检准确性
  - 像素到工作台毫米坐标的标定
  - STM32、XY、泵或喷头通信与执行
  - 真实清洗效果、损伤和硬件闭环

current_interfaces:
  version: mcl-v0
  path: microcleaning/contracts.py
  change_rule: "三人评审后才能修改；日常任务只消费接口。"

software_loop:
  status: framework_tested
  entry: microcleaning/app/replay_mcl.py
  controller: microcleaning/execution/fake_serial.py
  note: "框架可以用结构化前后数据回放，但尚无真实图像数据集和 OpenCV 算法证据。"

current_dataset:
  status: absent
  planned_baseline: "PLA 平面测试片 + 水溶性彩色标记物 + 固定俯视成像"

hardware_boundary:
  camera_port: microcleaning/adapters/ports.py
  controller_port: microcleaning/adapters/ports.py
  real_hardware_armed: false
  stm32_firmware_status: unavailable

development_environment:
  recommended_python: "CPython 3.12 + project-local .venv"
  base_profile: "Python 标准库，可运行 Mock/框架测试"
  perception_profile: "requirements/perception-opencv.txt，成员 A/B 开始真实图像任务时安装"
  serial_profile: "requirements/control-serial.txt，仅在已获协议后做受限串口适配"

file_ownership:
  member_a: "成像质量、回放图像入口和 A 测试"
  member_b: "污染测量、状态估计、复检和 B 测试"
  member_c: "动作申请、安全、FakeSerial、闭环编排、Episode 和 C 测试"
  shared: "contracts.py、AGENTS.md、project_state.yaml；需要评审"

next_actions:
  - "A：建立 30～50 张链路验证图像清单并实现真实图像质量评分。"
  - "B：实现 HSV 基线分割和前后图配准，报告失败样本。"
  - "C：补回放数据清单读取、FakeSerial 协议测试和 Episode 保存。"
  - "全体：每周用 5 组前后图跑一次完整回放并复盘失败。"
