"""Safety-bounded Minimum Closed Loop (MCL) package.

This package is intentionally separate from the legacy prototype.  Its mock
adapters never open a serial port, camera, pump, or motion controller.
"""

__version__ = "0.1.0"
